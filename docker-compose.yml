services:

  # ── Infrastruktur (startet immer) ─────────────────────────────────────────

  qdrant:
    image: qdrant/qdrant:latest
    volumes:
      - haana-media:/media/haana/qdrant:rw
    environment:
      QDRANT__STORAGE__STORAGE_PATH: /media/haana/qdrant/storage
    ports:
      - "6333:6333"   # REST API + Dashboard
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'cat < /dev/null > /dev/tcp/localhost/6333' 2>/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ── Agent-Instanzen (Profile: agents) ─────────────────────────────────────
  # Starten mit: docker compose --profile agents up -d

  instanz-alice:
    profiles: [agents]
    build: .
    env_file: .env
    environment:
      TZ: Europe/Berlin
      HAANA_INSTANCE: alice
      HAANA_API_PORT: 8001
      HAANA_MEDIA_DIR: /media/haana
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
    ports:
      - "8001:8001"
    volumes:
      - ./instanzen/alice/CLAUDE.md:/app/CLAUDE.md:ro
      - ./skills:/app/skills:ro
      - haana-data:/data
      - haana-media:/media/haana
    restart: unless-stopped
    depends_on:
      qdrant:
        condition: service_healthy

  instanz-bob:
    profiles: [agents]
    build: .
    env_file: .env
    environment:
      TZ: Europe/Berlin
      HAANA_INSTANCE: bob
      HAANA_API_PORT: 8002
      HAANA_MEDIA_DIR: /media/haana
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
    ports:
      - "8002:8002"
    volumes:
      - ./instanzen/bob/CLAUDE.md:/app/CLAUDE.md:ro
      - ./skills:/app/skills:ro
      - haana-data:/data
      - haana-media:/media/haana
    restart: unless-stopped
    depends_on:
      qdrant:
        condition: service_healthy

  # ha-assist + ha-advanced werden als System-User per AgentManager gestartet
  # (nicht per docker-compose), damit sie die korrekten LLM-Env-Vars aus der Config bekommen.
  # Image-Build: docker compose --profile agents build baut alice/bob — dasselbe Image.

  instanz-ha-assist:
    profiles: [build-only]
    build: .

  instanz-ha-advanced:
    profiles: [build-only]
    build: .

  instanz-haana-admin:
    profiles: [build-only]
    build: .

  # ── Admin-Interface (immer aktiv – kein Profil) ────────────────────────────

  admin-interface:
    build: ./admin-interface
    env_file: .env
    environment:
      TZ:                     Europe/Berlin
      HAANA_MEDIA_DIR:        /media/haana
      HAANA_DATA_DIR:         /data
      HAANA_CONF_FILE:        /data/config/config.json
      HAANA_INST_DIR:         /app/instanzen
      AGENT_URL_BENNI:        http://haana-instanz-alice-1:8001
      AGENT_URL_DOMI:         http://haana-instanz-bob-1:8002
      # Docker-Management für User-Provisioning
      HAANA_HOST_BASE:        /opt/haana
      HAANA_DATA_VOLUME:      haana_haana-data
      HAANA_COMPOSE_NETWORK:  haana_default
      HAANA_AGENT_IMAGE:      haana-instanz-alice  # wird beim ersten Start auto-detected
      WHATSAPP_BRIDGE_URL:    http://whatsapp-bridge:3001
      HAANA_ADMIN_PORT:       ${HAANA_ADMIN_PORT:-8080}
    ports:
      - "${HAANA_ADMIN_PORT:-8080}:${HAANA_ADMIN_PORT:-8080}"
    volumes:
      - ./instanzen:/app/instanzen
      - ./core:/app/core:ro
      - haana-data:/data
      - haana-media:/media/haana
      - /var/run/docker.sock:/var/run/docker.sock
      - /home/haana/.claude:/claude-auth
      - /home/haana/.local/bin/claude:/usr/local/bin/claude:ro
    restart: unless-stopped
    depends_on:
      qdrant:
        condition: service_healthy

  # ── WhatsApp-Bridge (Profil: agents) ──────────────────────────────────────

  whatsapp-bridge:
    profiles: [agents]
    build: ./whatsapp-bridge
    env_file: .env
    environment:
      ADMIN_URL:         http://admin-interface:8080
      BRIDGE_LOG_LEVEL:  ${BRIDGE_LOG_LEVEL:-info}
      BRIDGE_HTTP_PORT:  "3001"
      # HAANA_BRIDGE_SECRET: "your-secret"  # Optional: schützt /api/whatsapp-config
    volumes:
      - haana-whatsapp-session:/app/session
    restart: unless-stopped
    depends_on:
      - admin-interface

  # ── Optionale Dienste (auskommentiert bis konfiguriert) ───────────────────

  # trilium:
  #   image: triliumnext/notes:latest
  #   volumes:
  #     - haana-trilium:/home/node/trilium-data
  #   ports:
  #     - "8888:8888"
  #   restart: unless-stopped

  # ollama:
  #   image: ollama/ollama:latest
  #   volumes:
  #     - haana-ollama:/root/.ollama
  #   ports:
  #     - "11434:11434"
  #   restart: unless-stopped
  #   # GPU-Support:
  #   # deploy:
  #   #   resources:
  #   #     reservations:
  #   #       devices:
  #   #         - driver: nvidia
  #   #           count: 1
  #   #           capabilities: [gpu]

volumes:
  haana-data:
  haana-media:
  haana-whatsapp-session:
  # haana-trilium:
  # haana-ollama:
