# Code-Audit 2026-03-15

**Durchgeführt von:** Claude Code (Orchestrator)
**Scope:** core/, admin-interface/, tests/, channels/, skills/
**validate.sh:** 0 Fehler, 0 Warnungen (261 Tests)

---

## Kritisch (sofort fixen)

### 🔴 /api/wa-proxy/ — kein Auth

**Datei:** `admin-interface/main.py:215` + `admin-interface/routers/whatsapp.py:199`

`/api/wa-proxy/` steht in `_AUTH_EXEMPT_PREFIXES` und der `wa_proxy_chat` Handler hat **keinen** Bridge-Token-Check. Jeder, der den Admin-Interface-Port erreicht, kann beliebige Nachrichten als beliebiger User injizieren — inkl. `/admin` Slash-Command für Eskalation zu Admin-Modus.

**Fix:** Bridge-Token-Check analog zu `/api/wa/config` (Zeile 139–142) auch in `wa_proxy_chat` ergänzen.

---

## Warnung (diese Woche)

### 🟡 Dateigröße-Überschreitungen (>400 Zeilen)

| Datei | Zeilen | Status |
|-------|--------|--------|
| `admin-interface/static/js/config.js` | 2373 | 🔴 Kritisch (>600) |
| `core/memory.py` | 1548 | 🔴 Kritisch (>600) |
| `tests/test_ollama_compat.py` | 1272 | 🔴 Kritisch (>600) |
| `core/agent.py` | 1028 | 🔴 Kritisch (>600) |
| `admin-interface/static/js/wizard.js` | 1028 | 🔴 Kritisch (>600) |
| `tests/test_config.py` | 971 | 🔴 Kritisch (>600) |
| `core/ollama_compat.py` | 755 | 🔴 Kritisch (>600) |
| `core/process_manager.py` | 660 | 🟡 Warnung (>400) |
| `tests/test_agent.py` | 603 | 🔴 Kritisch (>600) |
| `admin-interface/routers/system.py` | 535 | 🟡 Warnung (>400) |
| `admin-interface/static/js/users.js` | 468 | 🟡 Warnung (>400) |
| `admin-interface/routers/claude_auth.py` | 452 | 🟡 Warnung (>400) |
| `tests/test_memory.py` | 443 | 🟡 Warnung (>400) |
| `admin-interface/static/js/logs.js` | 434 | 🟡 Warnung (>400) |
| `admin-interface/routers/logs.py` | 420 | 🟡 Warnung (>400) |

**Hinweis:** config.js (2373 Zeilen) ist bekannte Tech-Schuld. memory.py, agent.py und ollama_compat.py sind Kandidaten für das nächste Refactoring-Sprint.

### 🟡 host_claude_config Bug (bekannt, noch offen)

**Datei:** `core/process_manager.py:365`

```python
host_claude_config = "/home/haana/.claude"   # Zeile 365 - OK
claude_json_host = Path("/root/.claude.json")  # Zeile 404 - Bug: sollte /home/haana/.claude.json
```

Steht seit MS6 als bekanntes Problem in MEMORY.md. Noch nicht behoben.

---

## Technische Schuld (Backlog)

### 🔵 Fehlende Test-Coverage

Kritische Bereiche ohne Tests:
- `channels/whatsapp/channel.py` (kein Test-File)
- `admin-interface/routers/system.py` — `_do_update()` Funktion (535 Zeilen, komplex)
- `core/dream.py` — Dream-Catch-up Logik (nur Utilities und Process getestet)

### 🔵 Hardcoded localhost-Referenzen (akzeptabel, aber dokumentiert)

**Dateien:** `core/process_manager.py`, `core/ollama_compat.py`, `admin-interface/routers/deps.py`

```
core/process_manager.py:94  → "http://localhost:11434" (Fallback)
core/process_manager.py:613 → "http://localhost:8080/agent/{instance}"
admin-interface/routers/deps.py:65-67 → AGENT_URL_* mit localhost Defaults
```

Diese sind als Fallback-Defaults akzeptabel (alle via ENV oder Config überschreibbar). Kein direktes Sicherheitsproblem, aber bei Multi-Host-Setups zu beachten.

### 🔵 Globale OAuth-JS-Funktionen (toter Code)

`admin-interface/static/js/config.js` enthält alte globale OAuth-Funktionen, die UI nutzt inzwischen provider-scoped Endpunkte. Toter Code erhöht Dateigröße künstlich.

---

## Positiv ✅

### Lessons Learned — alle Checks bestanden

| Check | Status |
|-------|--------|
| mem0 `"version": "v1.1"` | ✅ `core/memory.py:291` |
| Absolute Pfade `/data` | ✅ Keine problematischen relativen Pfade |
| `save_context()` nach /chat + /ws | ✅ `core/api.py:57 + 117` |
| `TZ=Europe/Berlin` in allen Containern | ✅ `docker-compose.yml:47, 84` + `process_manager.py:184` |
| escHtml/escAttr bei innerHTML | ✅ `_llmSelectOpts()` korrekt mit `escAttr()` + `escHtml()` |
| Cache-Buster bei allen JS/CSS | ✅ Alle 14 JS-Dateien + CSS mit `?v=X` |
| i18n Parität DE/EN | ✅ Je 26 Keys, 0 Abweichungen |
| validate.sh | ✅ 0 Fehler, 0 Warnungen |
| Keine hardcodierten Usernames | ✅ Keine gefunden |
| Sensitive Daten in Logs | ✅ Token-Logging nur mit Länge/repr[:200], kein Plaintext |
| Bridge-Token Auth (/api/wa/config) | ✅ `secrets.compare_digest` korrekt |

### Neue Dateien (HEAD~10) — alle regelkonform

Alle 13 neuen/geänderten Dateien aus den letzten 10 Commits unter 400 Zeilen:
- `core/dream_prompts.py` (41 Zeilen)
- `core/dream_utils.py` (252 Zeilen)
- `admin-interface/static/js/status-agents.js` (64 Zeilen)
- `admin-interface/static/js/status-dream.js` (139 Zeilen)
- Dream-Refactoring durch Aufteilen erfolgreich

---

## Empfehlungen

1. **Sofort:** `wa_proxy_chat` Bridge-Token-Auth nachrüsten — einfacher 5-Zeilen-Fix
2. **Sprint-Kandidaten:** `core/memory.py` → aufteilen (mem0-layer / context / history), `core/agent.py` → aufteilen (tool-handling / delegation / core), `admin-interface/static/js/config.js` → Module extrahieren
3. **Nächster Audit:** Nach config.js-Refactoring die Zeilenzahl-Verteilung neu bewerten
4. **host_claude_config Bug:** Beim nächsten process_manager.py-Touch mitfixen (`/root/.claude.json` → `/home/haana/.claude.json`)

---

*Audit-Basis: git HEAD f1a3fa0, 2026-03-15*
