# HAANA – Umgebungsvariablen
# Kopiere diese Datei nach .env und fülle die Werte aus.
# Die .env-Datei wird NICHT ins Git eingecheckt.

# ── Anthropic API ─────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Home Assistant ────────────────────────────────────────────────────────────
HA_URL=http://homeassistant.local:8123
HA_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ── Optionaler Cloud-Fallback LLM (z.B. MiniMax) ─────────────────────────────
# FALLBACK_LLM_BASE_URL=https://api.minimaxi.chat/v1
# FALLBACK_LLM_API_KEY=
# FALLBACK_LLM_MODEL=MiniMax-Text-01

# ── Agent-Konfiguration ───────────────────────────────────────────────────────
# Welche Instanz läuft in diesem Container (alice | bob | ha-assist | ha-advanced)
HAANA_INSTANCE=alice
# Modell für den Agenten und für Mem0 Memory-Extraktion
HAANA_MODEL=claude-haiku-4-5-20251001

# ── Ollama (lokale Modelle + Embeddings) ──────────────────────────────────────
# Für bge-m3 Embeddings (empfohlen). Ohne diese Variable: HuggingFace-Fallback.
# OLLAMA_URL=http://192.168.1.50:11434

# ── Qdrant ────────────────────────────────────────────────────────────────────
QDRANT_URL=http://qdrant:6333

# ── Trilium (Wissensbasis) ────────────────────────────────────────────────────
# TRILIUM_URL=http://trilium:8888
# TRILIUM_TOKEN=

# ── WhatsApp Bridge ───────────────────────────────────────────────────────────
# WHATSAPP_BENNI_NUMBER=+49XXXXXXXXXX
# WHATSAPP_DOMI_NUMBER=+49XXXXXXXXXX

# ── Backup (TrueNAS / SMB) ────────────────────────────────────────────────────
# BACKUP_SMB_HOST=\\truenas\haana-backup
# BACKUP_SMB_USER=
# BACKUP_SMB_PASS=

# ── Erweitert ─────────────────────────────────────────────────────────────────
# ANONYMIZER_ENABLED=false
# LOG_LEVEL=INFO
